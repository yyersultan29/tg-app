# 🚀 Деплой Telegram Mini App

## Быстрый деплой на GitHub Pages (Рекомендуется)

### 1. Установите gh-pages

```bash
npm install --save-dev gh-pages
```

### 2. Добавьте в package.json

```json
{
  "homepage": "https://yourusername.github.io/your-repo-name",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
```

### 3. Обновите vite.config.ts

```typescript
export default defineConfig({
  base: '/your-repo-name/',
  plugins: [react()],
  // ... остальная конфигурация
})
```

### 4. Деплой

```bash
npm run deploy
```

### 5. Настройка GitHub Pages

1. Зайдите в Settings → Pages
2. Source: `gh-pages` branch
3. Ваш URL: `https://yourusername.github.io/your-repo-name`

### 6. Настройка бота в Telegram

1. Откройте [@BotFather](https://t.me/botfather)
2. Создайте нового бота: `/newbot`
3. Настройте Mini App: `/newapp`
4. Введите URL вашего GitHub Pages
5. Добавьте описание и иконку

---

## Альтернативные способы деплоя

### Vercel

1. **Установите Vercel CLI:**
```bash
npm i -g vercel
```

2. **Деплой:**
```bash
vercel
```

3. **Или через веб-интерфейс:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Подключите ваш GitHub репозиторий
   - Нажмите Deploy

### Netlify

1. **Установите Netlify CLI:**
```bash
npm i -g netlify-cli
```

2. **Деплой:**
```bash
npm run build
netlify deploy --prod
```

3. **Или через веб-интерфейс:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Подключите ваш GitHub репозиторий
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Файл `public/_redirects` уже настроен для роутинга

---

### Cloudflare Pages

1. Зайдите на [pages.cloudflare.com](https://pages.cloudflare.com)
2. Подключите GitHub репозиторий
3. Build command: `npm run build`
4. Build output directory: `dist`
5. Нажмите "Deploy"

---

## 🤖 Настройка Telegram Bot

### Создание бота

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/newbot`
3. Введите имя бота (например: "Burger King Order Bot")
4. Введите username бота (должен заканчиваться на "bot")

### Создание Mini App

1. В BotFather отправьте `/newapp`
2. Выберите вашего бота
3. Введите название приложения
4. Введите описание
5. Загрузите фото (512x512 px)
6. Загрузите GIF для демонстрации (необязательно)
7. Введите URL вашего деплоя (https://your-app.vercel.app)

### Настройка кнопки меню

1. В BotFather отправьте `/mybots`
2. Выберите вашего бота
3. Bot Settings → Menu Button
4. Выберите ваше Mini App

### Получение ссылки

После создания вы получите ссылку вида:
```
https://t.me/your_bot_name/your_app_name
```

---

## 🔧 Переменные окружения (если нужно)

Создайте файл `.env`:

```env
VITE_API_URL=https://your-api.com
VITE_BOT_TOKEN=your_bot_token
```

Используйте в коде:

```typescript
const apiUrl = import.meta.env.VITE_API_URL;
```

---

## 📋 Чеклист перед деплоем

- [ ] Проверьте что `npm run build` работает без ошибок
- [ ] Протестируйте production build локально: `npm run preview`
- [ ] Убедитесь что все пути корректны
- [ ] Проверьте что Telegram WebApp API интегрирован
- [ ] Проверьте мобильную версию
- [ ] Оптимизируйте изображения (если есть)
- [ ] Настройте HTTPS (обязательно для Telegram Mini Apps)

---

## 🐛 Решение проблем

### Белый экран после деплоя

**Проблема:** Base path не настроен  
**Решение:** Проверьте `base` в `vite.config.ts`

### Telegram API не работает

**Проблема:** Приложение открыто не через Telegram  
**Решение:** Добавьте fallback значения в `useTelegramTheme.ts`

### Роутинг не работает на продакшене

**Проблема:** SPA routing не настроен  

**Решение для GitHub Pages:** Используйте HashRouter вместо BrowserRouter в `App.tsx`:

```typescript
import { HashRouter as Router } from 'react-router-dom';
// вместо
import { BrowserRouter as Router } from 'react-router-dom';
```

**Решение для Netlify:** Файл `public/_redirects` уже создан:

```
/*    /index.html   200
```

---

## 📱 Тестирование

### Локальное тестирование с ngrok

1. Установите ngrok: https://ngrok.com/
2. Запустите приложение: `npm run dev`
3. Создайте туннель: `ngrok http 5173`
4. Используйте HTTPS URL от ngrok в BotFather

### Тестирование на мобильном

1. Откройте Telegram на телефоне
2. Найдите вашего бота
3. Нажмите на кнопку меню
4. Приложение должно открыться в Telegram

---

## 🎉 Готово!

Ваше приложение теперь доступно в Telegram!

Поделитесь ссылкой:
```
https://t.me/your_bot_name/your_app_name
```

Или используйте прямую ссылку:
```
https://t.me/your_bot_name?start=app
```

